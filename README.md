# nvim config
basic neovim config

# .bash_aliases
```ln -s .bash_nvim ~/```
add the following line to ~/.bash_aliases
```source ~/.bash_nvim```

# git
git remote set-url origin git@github.com:clneagu/nvim.git
